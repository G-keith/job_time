server:
  port: 9099 #端口
spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://111.231.50.113:3306/job_time?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&serverTimezone=GMT%2B8
      username: root
      password: manager
      initialSize: 1  #初始化大小
      minIdle: 1  #空闲连接池的大小
      maxActive: 50 #最大激活数量
  servlet:
    multipart:
      max-file-size: 50MB #单个文件上传大小限制
      max-request-size: 500MB #单次请求上传文件大小限制
#MyBatis配置文件
mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mappers/**/*.xml
#日志打印SQL
logging:
  level:
     com.emes: debug  #日志级别
  config: classpath:logback-dev.xml  #加载指定日志文件
system:
  domainName: http://127.0.0.1 #域名
  picPath: /opt/tomcat/webapps/mes  #上传图片路径
swagger:
  enable: true #是否开启swagger

fileSavePath: C:\photo

resourcePath: /resource